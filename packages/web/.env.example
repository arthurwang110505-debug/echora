# Spotify Developer Dashboard → Folia app → Redirect URI
VITE_SPOTIFY_CLIENT_ID=
VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:3000/app

# Google Cloud Console →「API 和服務」→ 憑證 → OAuth 用戶端 ID（Web 應用程式）。
# 用於 YouTube Music 官方 OAuth 登入。設定前請完成三件事：
#   1. 專案已啟用「YouTube Data API v3」（API 和服務 → 媒體庫），否則登入後 API 會回 403。
#   2. OAuth 用戶端的「已授權的 JavaScript 來源」包含部署網址 origin
#      （本機 http://localhost:3000、正式環境 https://你的網域.vercel.app）。
#   3. OAuth 同意畫面若為「測試中」狀態，需將要登入的 Google 帳號加入測試使用者。
VITE_GOOGLE_CLIENT_ID=
# 選填：明確指定 OAuth 導回網址；預設為 {目前網址 origin}/oauth/youtube/callback
VITE_GOOGLE_REDIRECT_URI=

# Google Search Console「HTML 標記」驗證用的 content 值（可留空）。
# 建置時由 vite-plugins/siteMetadata.ts 注入 <meta name="google-site-verification">，
# dev 與 production build 都會生效。設定步驟見 docs/google-oauth-brand-verification.md。
VITE_GOOGLE_SITE_VERIFICATION=

# 選填：canonical / Open Graph / sitemap.xml 使用的站台網址（只需 origin）。
# 預設為 https://echora-three.vercel.app；綁自訂網域時改成 https://你的網域。
VITE_SITE_URL=

# Optional: Google Cloud YouTube Data API v3 key for public music search.
# Server-side only — the browser calls /api/youtube/search, so the key never ships
# to the client. Do not prefix this with VITE_. (Private playlists and playback
# still use the official OAuth Data API; public search uses this key.)
YOUTUBE_API_KEY=

# Agnes AI is server-side only. Do not prefix this with VITE_ or expose it in the browser.
# For Vercel, configure AGNES_API_KEY in Project Settings → Environment Variables.
AGNES_API_KEY=
